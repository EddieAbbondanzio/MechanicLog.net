/**
 * Calculation methods for maintenance cost.
 */
export enum MaintenanceCostCalculationMethod {
    /**
     * Calculate the cost by adding up each line.
     */
    Line = 0,
    /**
     * Cost was calculated using user input.
     */
    Manual = 1,
}
